---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Safe Settings

on:
  push:
    branches:
      - main
    paths:
      - .github/safe-settings/**
      - .github/safe-settings-deployment.yml
      - .github/workflows/safe-settings.yaml
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:
    inputs:
      dryRun:
        description: Dry Run
        type: boolean
        default: false

permissions:
  contents: read

env:
  # renovate: datasource=github-releases depName=github/safe-settings
  SAFE_SETTINGS_VERSION: 2.1.18
  SAFE_SETTINGS_DIR: ${{ github.workspace }}/.safe-settings

jobs:
  full-sync:
    name: Reconcile org repository settings
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Checkout safe-settings
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: github/safe-settings
          ref: ${{ env.SAFE_SETTINGS_VERSION }}
          path: ${{ env.SAFE_SETTINGS_DIR }}
          persist-credentials: false

      - name: Setup Mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          experimental: true
          install_args: --locked

      - name: Install safe-settings
        working-directory: ${{ env.SAFE_SETTINGS_DIR }}
        run: npm ci

      - name: Run safe-settings full-sync
        working-directory: ${{ env.SAFE_SETTINGS_DIR }}
        env:
          GH_ORG: ${{ github.repository_owner }}
          # Public GitHub App ID for woodmin; the private key remains secret.
          APP_ID: "1741173"
          PRIVATE_KEY: ${{ secrets.BOT_APP_PRIVATE_KEY }}
          ADMIN_REPO: .github
          CONFIG_PATH: .github/safe-settings
          DEPLOYMENT_CONFIG_FILE: ${{ github.workspace }}/.github/safe-settings-deployment.yml
          FULL_SYNC_NOP: ${{ inputs.dryRun || 'false' }}
        run: npm run full-sync
