---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Safe Settings

on:
  push:
    branches:
      - main
    paths:
      - .github/safe-settings/**
      - .github/workflows/safe-settings.yaml
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:
    inputs:
      dryRun:
        description: Dry Run
        type: boolean
        default: false

permissions:
  contents: read

env:
  # renovate: datasource=github-releases depName=github/safe-settings
  SAFE_SETTINGS_VERSION: 2.1.21
  SAFE_SETTINGS_DIR: ${{ github.workspace }}/.safe-settings

jobs:
  full-sync:
    name: Reconcile org repository settings
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout safe-settings
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: github/safe-settings
          ref: ${{ env.SAFE_SETTINGS_VERSION }}
          path: ${{ env.SAFE_SETTINGS_DIR }}
          persist-credentials: false

      - name: Setup Mise
        uses: jdx/mise-action@3c2e0cf82a5b2e5249f0d3635a4d83d0ae861518 # v4.2.5
        with:
          experimental: true
          install_args: --locked

      - name: Install safe-settings
        working-directory: ${{ env.SAFE_SETTINGS_DIR }}
        run: npm ci

      - name: Generate Token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ secrets.BOT_CLIENT_ID }}
          private-key: ${{ secrets.BOT_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          permission-metadata: read

      # Reconcile every active repository that inherits the shared Renovate
      # preset. Archived repositories are discovered on every run because
      # safe-settings cannot PATCH them.
      - name: Generate deployment-settings.yml
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          OWNER: ${{ github.repository_owner }}
        run: |
          {
            echo '---'
            echo 'restrictedRepos:'
            echo '  include:'
            echo '    - .github'
            echo '    - metabasis'
            echo '    - SetRecoveryPassword'
            echo '    - TagPrint'
            echo '    - assetcache-exporter'
            echo '    - autopkg'
            echo '    - autopkg-gitops'
            echo '    - epson-exporter'
            echo '    - freshservice-label'
            echo '    - grinch'
            echo '    - leave-calendar'
            echo '    - onomazo'
            echo '    - renovate-config'
            echo '    - selectronic-exporter'
            echo '    - talaria'
            echo '    - woodgate'
            echo '    - woodstar'
            echo '    - woodsweep'
            echo '  exclude:'
            gh repo list "$OWNER" --limit 200 --json name,isArchived \
              --jq '.[] | select(.isArchived) | "    - " + .name'
          } > deployment-settings.yml
          cat deployment-settings.yml

      - name: Run safe-settings full-sync
        working-directory: ${{ env.SAFE_SETTINGS_DIR }}
        env:
          GH_ORG: ${{ github.repository_owner }}
          # Public GitHub App ID for woodmin; the private key remains secret.
          APP_ID: "1741173"
          PRIVATE_KEY: ${{ secrets.BOT_APP_PRIVATE_KEY }}
          ADMIN_REPO: .github
          CONFIG_PATH: .github/safe-settings
          DEPLOYMENT_CONFIG_FILE: ${{ github.workspace }}/deployment-settings.yml
          FULL_SYNC_NOP: ${{ inputs.dryRun || 'false' }}
        run: npm run full-sync
